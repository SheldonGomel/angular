import { DateBackgroundDirective } from './date-background.directive';

describe('DateBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DateBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
